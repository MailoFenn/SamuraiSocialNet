import React from 'react';
import logo from '../logo2.jpg'

const Header = () => {
    return(
        <header>
            <img src={logo} alt="logo"/>
        </header>
    );
}

export default Header;